/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/travel-app",
  output: "export",
  images: { unoptimized: true },
  reactStrictMode: true,
};

module.exports = nextConfig;
